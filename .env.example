BOT_TOKEN=
TRANSAK_API_KEY=
TRANSAK_API_SECRET=
TREASURY_WALLET=0x57f333c398c9625D84432aBD00871E2d8049cAaC
REFERRER_DOMAIN=https://frontemeridionale.github.io
TRANSAK_REFRESH_TOKEN_URL=
TRANSAK_CREATE_WIDGET_URL=

# Environment: "staging" (default) or "production"
ENVIRONMENT=staging

# CORS: comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=https://frontemeridionale.github.io,http://localhost:3000

# Rate limiting: max requests per minute per IP (default: 30)
RATE_LIMIT_PER_MINUTE=30

# Server
HOST=0.0.0.0
PORT=5000
